import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    main: {
        marginTop: 10,
        padding: 0,
        flexGrow: 1,
        display: 'flex',
        flexDirection: 'column',
    }
}));
